[package]
name = "shph-benchmarks"
version = "0.6.1-dev"
edition = "2021"
publish = false

[workspace]

[dependencies]
libc = "0.2"
shph-core = { path = "../shph-core" }
shph-transport = { path = "../shph-transport" }
